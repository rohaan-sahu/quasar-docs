---
title: IDL Generation
description: How Quasar generates program interface descriptions and client code from your Rust source.
---

The IDL is a JSON file describing your program's interface: instructions, accounts, events, types, and errors. Generated from your Rust source, it drives TypeScript and Rust client generation.

## What the IDL Contains

Top-level structure:

```json
{
  "address": "22222222222222222222222222222222222222222222",
  "metadata": {
    "name": "quasar_escrow",
    "version": "0.1.0",
    "spec": "0.1.0"
  },
  "instructions": [ ... ],
  "accounts": [ ... ],
  "events": [ ... ],
  "types": [ ... ],
  "errors": [ ... ]
}
```

### Instructions

Each instruction includes name, discriminator bytes, accounts with flags, and typed arguments:

```json
{
  "name": "Make",
  "discriminator": [0],
  "accounts": [
    { "name": "maker", "writable": true, "signer": true },
    { "name": "escrow", "writable": true, "pda": {
      "seeds": [
        { "kind": "const", "value": [101, 115, 99, 114, 111, 119] },
        { "kind": "account", "path": "maker" }
      ]
    }},
    { "name": "mintA" },
    { "name": "mintB" },
    { "name": "makerTaA", "writable": true },
    { "name": "makerTaB", "writable": true },
    { "name": "vaultTaA", "writable": true },
    { "name": "rent", "address": "SysvarRent111111111111111111111111111111111" },
    { "name": "tokenProgram", "address": "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" },
    { "name": "systemProgram", "address": "11111111111111111111111111111111" }
  ],
  "args": [
    { "name": "deposit", "type": "u64" },
    { "name": "receive", "type": "u64" }
  ]
}
```

PDA seeds are encoded as `const` (literal bytes) or `account` (reference to another account). `writable` and `signer` flags appear only when `true`. Known program addresses are included as `address` fields. `CtxWithRemaining` instructions set `hasRemaining: true`.

### Accounts

Each `#[account]` struct lists its discriminator:

```json
{
  "name": "Escrow",
  "discriminator": [1]
}
```

### Events

Same pattern as accounts:

```json
{
  "name": "MakeEvent",
  "discriminator": [0]
}
```

Event discriminators are prefixed with `0xFF` on-chain to distinguish them from instructions. See [CPI](./cpi) for event emission details.

### Types

Struct types referenced by instructions or accounts:

```json
{
  "name": "Escrow",
  "type": {
    "kind": "struct",
    "fields": [
      { "name": "maker", "type": "pubkey" },
      { "name": "mintA", "type": "pubkey" },
      { "name": "mintB", "type": "pubkey" },
      { "name": "makerTaB", "type": "pubkey" },
      { "name": "receive", "type": "u64" },
      { "name": "bump", "type": "u8" }
    ]
  }
}
```

#### Dynamic Field Types

Dynamic fields have dedicated IDL representations:

```json
{ "name": "label", "type": { "string": { "maxLength": 32 } } }
{ "name": "signers", "type": { "vec": { "items": "pubkey", "maxLength": 10 } } }
{ "name": "data", "type": { "tail": { "element": "u8" } } }
```

- **`string`**: Maps to `String<P, MAX>` in Rust (e.g., `String<u8, 32>`). Includes the maximum byte length.
- **`vec`**: Maps to `Vec<T, P, MAX>` (e.g., `Vec<Address, u32, 10>`). Includes the item type and maximum element count.
- **`tail`**: Maps to a bare `&[u8]` or `&str` in Rust. Trailing data with no length prefix.

### Errors

`#[error_code]` variants are numbered from 0 by default (or from a custom base if the first variant specifies one). Framework errors (`QuasarError`) start at 3000.

```json
{
  "code": 6000,
  "name": "InvalidAmount",
  "msg": "Invalid amount"
}
```

## Supported Primitive Types

Primitive types map directly to Rust:

| IDL Type | Rust Type | Size |
|----------|-----------|------|
| `u8` | `u8` | 1 byte |
| `i8` | `i8` | 1 byte |
| `bool` | `bool` | 1 byte |
| `u16` | `u16` | 2 bytes |
| `i16` | `i16` | 2 bytes |
| `u32` | `u32` | 4 bytes |
| `i32` | `i32` | 4 bytes |
| `u64` | `u64` | 8 bytes |
| `i64` | `i64` | 8 bytes |
| `u128` | `u128` | 16 bytes |
| `i128` | `i128` | 16 bytes |
| `pubkey` | `Address` | 32 bytes |

## Generating the IDL

Generate the IDL and client code:

```bash
quasar idl path/to/program
```

### Output Structure

```
target/
  idl/
    my_program.idl.json              # The IDL JSON file
  client/
    typescript/
      my_program/
        web3.ts                      # TypeScript client (web3.js v2)
        kit.ts                       # TypeScript client (@solana/kit)
        package.json                 # npm package manifest
    rust/
      my-program-client/
        Cargo.toml                   # Standalone Rust crate
        src/
          lib.rs                     # Typed instruction builders
```

### TypeScript Clients

- **`web3.ts`**: Uses `@solana/web3.js` v2.
- **`kit.ts`**: Uses `@solana/kit`.

Both include typed instruction builders, account/event type definitions, discriminator constants, and codec functions.

### Rust Client

A standalone crate with typed `Instruction` builders. Each instruction struct implements `From<T> for Instruction`:

```rust
pub struct DepositInstruction {
    pub user: Address,
    pub vault: Address,
    pub system_program: Address,
    pub amount: u64,
}

let ix: Instruction = DepositInstruction {
    user: user_pubkey,
    vault: vault_pda,
    system_program: system_program::ID,
    amount: 1_000_000,
}.into();
```

## Compile-Time IDL Consumption with declare_program!

`declare_program!` consumes an IDL JSON at compile time to generate typed CPI helpers:

```rust
declare_program!(vault_program, "target/idl/quasar_vault.idl.json");
```

See [CPI](./cpi) for the generated module structure.

## Discriminator Collision Detection

All instruction discriminators must be unique and have the same byte length (enforced at compile time). Event discriminators occupy a separate namespace (prefixed with `0xFF`) and do not conflict with instructions.

## Next Steps

- [Program Structure](./program-structure) — how the `#[program]` macro generates the dispatch table
- [Instructions](./instructions) — discriminators and argument handling
- [CPI](./cpi) — using `declare_program!` for typed cross-program calls
